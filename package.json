{
  "name": "beefast-utils",
  "version": "0.0.1",
  "description": "",
  "keywords": ["ts","js","utils"],
  "main": "dist/beefast-utils.umd.js",
  "module": "dist/beefast-utils.es.js",
  "exports": {
    ".": {
      "require": "./dist/beefast-utils.umd.js",
      "import": "./dist/beefast-utils.es.js"
    }
  },
  "typings": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc && vite build && tsc -P tsconfig.dts.json",
    "test": "jest --coverage",
    "prepare": "yarn build",
    "format": "prettier --ignore-path .gitignore --write .",
    "format:check": "prettier --ignore-path .gitignore --check ."
  },
  "devDependencies": {
    "@types/jest": "^26.0.22",
    "jest": "^26.6.3",
    "prettier": "^2.2.1",
    "prettier-plugin-sorted": "^2.0.0",
    "ts-jest": "^26.5.4",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.3",
    "vite": "^2.1.5"
  }
}
